<div class="announce">
  <span class="announce__tag">Novità</span>
  <span class="announce__text">Moonbrand ora genera anche i visivi per i caroselli Instagram.</span>
  <a class="announce__link" href="#come-funziona">Scopri come <span aria-hidden="true">→</span></a>
</div>

<style>
  .announce {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 11px var(--gutter);
    background: var(--ink);
    color: #fff;
    font-size: 14.5px;
    text-align: center;
  }

  .announce__tag {
    padding: 4px 10px;
    border-radius: 999px;
    background: rgb(255 255 255 / 0.16);
    font-family: var(--font-display);
    font-size: 11.5px;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .announce__text {
    color: rgb(255 255 255 / 0.88);
  }

  .announce__link {
    color: #fff;
    font-weight: 600;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .announce__link:hover {
    color: var(--accent-soft);
  }
</style>
